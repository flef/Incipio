{% extends "::layout.html.twig" %}

{% block title %}Suivi:{% endblock %}

{% block content %}

    {# On définit un sous-titre commun à toutes les pages du bundle, par exemple #}
    {% block sous_titre %}
        <h1>Suivi d'étude</h1>
        <h4>Sous-Menu</h4>
        <ul>
            <li><a href="{{ path('mgateSuivi_etude_homepage') }}">Accueil</a></li>
            <li><a href="{{ path('mgateSuivi_etude_ajouter') }}">Ajouter une étude</a></li>
        </ul>
        <hr>
    {% endblock %}

    {# On définit un nouveau block, que les vues du bundle pourront remplir #}
    {% block content_bundle %}
    {% endblock %}

{% endblock %}