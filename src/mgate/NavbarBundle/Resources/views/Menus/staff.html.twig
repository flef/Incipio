<ul class="nav">
    <!-- GESTION ASSOCIATIVE -->
    {% if is_granted('ROLE_SUIVEUR') %}
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Gestion Associative <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li class="nav-header">Membres</li>
            <li><a href="{{ path('mgatePersonne_membre_homepage') }}"><i class="icon-list"></i> Lister les membres</a></li>
            <li><a href="{{ path('mgatePersonne_membre_ajouter') }}"><i class="icon-plus"></i> Ajouter un membre</a></li>
        </ul>
    </li>
    {% endif %}
    
    <!-- RECRUTEMENT FORMATIONS PASSATION -->
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Formations <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li class="nav-header">Formations</li>
            <li><a href="{{ path('mgate_formations_lister') }}"><i class="icon-briefcase"></i> Voir les formations</a></li>
            {% if is_granted('ROLE_CA') %}
            <li class="nav-header">Administration</li>
            <li><a href="{{ path('mgate_formations_index_admin') }}"><i class="icon-list"></i> Gérer les formations</a></li>
            <li><a href="{{ path('mgate_formation_ajouter') }}"><i class="icon-plus"></i> Ajouter une formation</a></li>
            {% endif %}
        </ul>
    </li>

    {% if is_granted('ROLE_SUIVEUR') %}
    <!-- SUIVI D'ETUDES-->
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Suivi d'Études <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li><a href="{{ path('mgateSuivi_etude_homepage') }}"><i class="icon-list"></i> Voir les études</a></li>
            <li><a href="{{ path('mgateSuivi_etude_ajouter') }}"><i class="icon-plus"></i> Créer une étude</a></li>
        {% if etudesSuiveur | length %}
            <li class="nav-header">Mes Etudes:</li>
            {% for etude in etudesSuiveur %}
            <li><a href="{{ path('mgateSuivi_etude_voir', {'id': etude.id}) }}"><i class="icon-thumbs-up"></i> {{ getRefEtude(etude) }}</a></li>
            {% endfor%}
        {% endif %}
            {% if is_granted('ROLE_CA') %}
            <li class="nav-header">Administration</li>
            <li><a href="{{ path('mgateSuivi_vu_ca') }}"><i class="icon-eye-open"></i> Vu CA</a></li>
            <li><a href="{{ path('mgateSuivi_etude_suivi') }}"><i class="icon-calendar"></i> Récapitulatif</a></li>
            {% endif %}
        </ul>
    </li>
    {% endif %}
    
    <!-- TRESORERIE-->
    {% if is_granted('ROLE_CA') %}
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Trésorerie<b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li><a href="{{ path('mgate_treso_urssaf') }}"><i class="icon-info-sign"></i> URSSAF</a></li>
        </ul>
    </li>
    {% endif %}
    
    <!-- DEVELOPPEMENT COMMERCIAL -->
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Prospection <b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li class="nav-header">Prospect/Client</li>
            <li><a href="{{ path('mgatePersonne_prospect_homepage') }}"><i class="icon-list"></i> Voir les prospects</a></li>
            <li><a href="{{ path('mgatePersonne_prospect_ajouter') }}"><i class="icon-plus"></i> Ajouter un prospect</a></li>
        {% if is_granted('ROLE_CA') %}
            <li class="nav-header">Annuaire</li>
            <li><a href="{{ path('mgatePersonne_annuaire') }}"><i class="icon-book"></i> Annuaire global</a></li>
            <li><a href="{{ path('mgatePersonne_listeDiffusion') }}"><i class="icon-envelope"></i> Liste de diffusion</a></li>
        {% endif %}
        </ul>
    </li>
    
    <!-- PILOTAGE DE LA J.E. -->
    {% if is_granted('ROLE_CA') %}
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Pilotage de la J.E.<b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li><a href="{{ path('mgate_indicateurs_index') }}"><i class="icon-globe"></i> Indicateur</a></li>
        </ul>
    </li>
    {% endif %}
    
    <!-- ADMINISTRATION DU SITE -->
    {% if is_granted('ROLE_ADMIN') %}
    <li class="dropdown">
        <a class="dropdown-toggle disabled" data-toggle="dropdown" data-hover="dropdown">Administration<b class="caret"></b></a>
        <ul class="dropdown-menu">
            <li><a href="{{ path('mgate_publi_documenttype_index') }}"><i class="icon-wrench"></i> Gestion des Doctypes</a></li>
            <li class="nav-header">Postes</li>
            <li><a href="{{ path('mgatePersonne_poste_homepage') }}"><i class="icon-list"></i> Lister les postes</a></li>
            <li><a href="{{ path('mgatePersonne_poste_ajouter') }}"><i class="icon-plus"></i> Ajouter un poste</a></li>
            <li class="nav-header">Compte d'Utilisateur:</li>
            <li><a href="{{ path('mgate_user_lister') }}"><i class="icon-pencil"></i> Lister / Modifier</a></li>
        </ul>
    </li>
    {% endif %}
</ul>