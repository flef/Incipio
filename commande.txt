
cd D:/Data/Docs/Assos/M-GaTE/site/My-M-GaTE
 D:

 ne pas hesiter a supprimer des dossier de vendor
installation de composer :
curl -s https://getcomposer.org/installer | php
php composer.phar self-update
php composer.phar install                   <- bof a ne pas faire update c'est mieux
php composer.phar update
php -d memory_limit=-1 composer.phar update



Supprimer le Cache
php -d memory_limit=-1 app/console cache:clear --env=prod
php app/console cache:clear

chmod -R 777 app/cache
chmod -R 777 app/logs

Pour installer un truc, ça fait des copies dans /web
php app/console assets:install --symlink web		necessite droit admin
php app/console assets:install web                   pour jQuery
php app/console assetic:dump --env=prod --no-debug   pour commentbundle

Générateur de bundle / entity
php app/console generate:bundle  (mgate/PubliBundle, enter, enter, yml, no)
php app/console generate:doctrine:entity  (annotation)

Génération / Mise à jour get et setter
php app/console doctrine:generate:entities mgatePersonneBundle:Prospect

Mettre a jour la base de donnée
php app/console doctrine:database:drop --force
php app/console doctrine:database:create
php app/console doctrine:schema:update --dump-sql
php app/console doctrine:schema:update --force

Générer le super administrateur :
php app/console doctrine:fixtures:load --append
	
Afficher nimporte quel type de variable	
	var_dump() 

	
php app/console router:debug

php app/console generate:doctrine:crud
yml pour les route attention


php app/console fos:user:promote

service liste
php app/console container:debug




Create a new repository on the command line
touch README.md
git init
git add README.md
git commit -m "first commit"
git push -u origin master

Push an existing repository from the command line
git remote add origin https://github.com/stephanecollot/Symfony.git
git push -u origin master

update tracked files:
git add -u

restaure les fichier sur le derniere commit
git reset --hard HEAD

pour restaurer un fichier :
git checkout monFichier.txt  ou git reset HEAD monFichier.txt

        
        $logger = $this->get('logger');
        $logger->info('I just got the logger !!!!!!!!!!!!!!!!!!!!!! ');
        $logger->info(var_export($this->get('request'), true));
 

SET NAMES UTF-8


WAMPSERVEUR Apache 2.4.2 et PHP 5.4.3
wamp > apache > apache module > ssl_module
restart wamp
wamp > php > extension > intl

créer les dossier "app/cache/" "app/logs/" 
